# vaults/

Local story vaults for testing. **Everything in this directory is gitignored**
except this README and `.gitkeep`.

Create one:

```bash
pnpm vault:new my-story
```

Then run `/init` inside litfire to scaffold it.

Nothing here is ever committed, so it is safe to put real prose, real character
notes, and real experiments in a vault under this directory.
